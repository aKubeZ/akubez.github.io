class Matrix {
    constructor(m, n) {
        this.matrix = document.createElement('div');
        this.matrix.classList.add('matrix');

        for (let i = 1; i <= m; i++) {
            for (let j = 1; j <= n; j++) {
                // const numEntry = document.createEE
            }
        }
    }
}